{
  "name": "fhirgate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "export $(grep -v '^#' .env | xargs) && next dev -p $PORT",
    "build": "next build",
    "start": "export $(grep -v '^#' .env | xargs) && next start -p $PORT",
    "lint": "eslint"
  },
  "dependencies": {
    "lucide-react": "^1.7.0",
    "next": "16.2.1",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.1",
    "typescript": "^5"
  }
}
